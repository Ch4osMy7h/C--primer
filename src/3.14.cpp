#include<iostream>
#include<string>
#include<cctype>
#include<vector>
using namespace::std;
int main(void)
{
    int i;
    vector<int> v1;
    while(cin>>i)
    {
        v1.push_back(i);
    }
    return 0;
}
